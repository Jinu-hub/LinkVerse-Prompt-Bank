{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "daily-market-summary-posting-v1-output",
  "type": "string",
  "minLength": 1,
  "description": "Complete SNS post text in the language specified by lang_code. Plain text only — not JSON, not markdown-wrapped."
}
