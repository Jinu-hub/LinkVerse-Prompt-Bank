{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "sns-market-memory-footer-v1-output",
  "type": "string",
  "minLength": 1,
  "description": "Market Memory CTA footer tailored to the finished SNS post. Plain text only — not JSON, not markdown-wrapped. Must include exactly one 'Market Memory', a 🔗 profile/pinned-post line, and a 🔍 Google search line for 'marketmemory app'. No raw URLs or trailing separators."
}
