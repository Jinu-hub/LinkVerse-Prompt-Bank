{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "sns-human-voice-writer-v2-output",
  "type": "string",
  "minLength": 1,
  "description": "Rewritten SNS post text in the language specified by lang_code. Plain text only — not JSON, not markdown-wrapped, with no editorial labels, notes, or commentary. Useful numbered sections, headings, visual boundaries, and the strongest analytical ending from source_text must be preserved. Existing CTA, footer, separator, and URLs from source_text must also be preserved."
}
