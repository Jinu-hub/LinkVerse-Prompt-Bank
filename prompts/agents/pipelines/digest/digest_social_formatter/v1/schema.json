{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "DigestSocialFormatterOutput",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "output": {
      "type": "string",
      "minLength": 1,
      "description": "SNS-ready plain text: Markdown removed, section order preserved (Header → Intro → Highlights → Items → More Items → Closing → Glossary), with separators and symbols per system prompt. No JSON or markdown fences inside this string."
    }
  },
  "required": ["output"]
}
