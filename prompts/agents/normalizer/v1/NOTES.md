# Normalizer v1

## Agent metadata

| key | value |
|-----|-------|
| id | normalizer |
| version | v1 |
| name | Normalizer |
| description | Preprocessing agent: normalize raw document text (structural cleanup only). Do not interpret meaning. |

**tags:** normalization, preprocessing, document, extraction

| contract | detail |
|----------|--------|
| input | raw document text (SOURCE_TEXT) |
| output | clean_text, block_structure, removed_noise_log |
| response_format | json |

## 변경 이력

- **왜 수정했는지**: (수정 시 여기에 기록)
- **어떤 문제가 있었는지**: (이슈 발생 시 기록)
- **어떤 예시에서 실패했는지**: (실패한 input/expected 있으면 기록)
